//* CLASS TEMPLATES

template<typename T>
class BoxContainer {

  public:
    BoxContainer( ) { }

    ~BoxContainer( ) { }

    // Methods of clas templates should atleast be declared (if not defined), in C++ header files.
    // Otherwise the linker won't be able to find the class methods during compilation and we will
    // get undefined reference errors.

};